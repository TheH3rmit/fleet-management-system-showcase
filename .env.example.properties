# Shared DB/app settings (example values)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fleet
DB_USER=postgres
DB_PASS=admin
SPRING_PROFILES_ACTIVE=dev

# Optional: app port
APP_PORT=8080

# Secrets (JWT must be at least 32 chars)
JWT_SECRET=replace-me-32-chars-minimum-xxxxxxxxxxxx

# Frontend -> backend (docker network)
BACKEND_URL=http://backend:8080
